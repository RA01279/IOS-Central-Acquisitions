# Supabase (shared project with RIDGE Intel, or standalone -- see architecture notes)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Microsoft Entra ID (Azure AD) -- team SSO + delegated Mail.Send for
# MLA request notifications. Needs an app registration approved by
# Dalfen IT, same review track as RIDGE Intel's Azure access.
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# Comma-separated emails allowed to confirm "Moving to PSA"
PSA_CONFIRM_ALLOWLIST=rhett@dalfen.com,john@dalfen.com
